# Copy this file to "keystore.properties" (gitignored) and fill in real values.
# The release build reads these to sign the app bundle. Keep the real file and
# the .jks keystore backed up somewhere safe and private.
storeFile=upload-keystore.jks
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=upload
keyPassword=YOUR_KEY_PASSWORD
